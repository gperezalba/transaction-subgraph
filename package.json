{
  "name": "transaction-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gperezalba/transaction-subgraph",
    "create-local": "graph create --node http://localhost:8020/ gperezalba/transaction-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ gperezalba/transaction-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gperezalba/transaction-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
